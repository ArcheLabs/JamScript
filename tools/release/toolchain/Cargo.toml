[package]
name = "jamscript-toolchain-release-helper"
version = "0.1.0"
edition = "2021"
publish = false
# Keep the installation verifier as the package default even though this
# crate also ships auxiliary binaries such as `compress-zstd`.
default-run = "jamscript-toolchain-release-helper"

[workspace]

[dependencies]
anyhow = "1.0.100"
jamscript-toolchain = { path = "../../../crates/jamscript-toolchain" }
toml = "0.8.23"
zstd = "0.13.3"
