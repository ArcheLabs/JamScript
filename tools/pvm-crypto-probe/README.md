# Crypto PolkaVM probe

The crypto probe covers the no-std signing/verification primitives used by a
generated service. Its acceptance artifact is the canonical `service.elf`
plus the converter output; no host-only crypto implementation is accepted as
a substitute.
