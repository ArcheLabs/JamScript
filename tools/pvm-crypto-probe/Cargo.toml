[package]
name = "pvm-crypto-probe"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jamscript-crypto = { path = "../../crates/jamscript-crypto", default-features = false }
schnorrkel = { version = "0.11.5", default-features = false }
polkavm-derive = "=0.30.0"

[workspace]
