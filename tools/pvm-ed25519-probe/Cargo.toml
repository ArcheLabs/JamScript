[package]
name = "pvm-ed25519-probe"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ed25519-dalek = { version = "2.2.0", default-features = false, features = ["fast"] }
polkavm-derive = "=0.30.0"

[workspace]
